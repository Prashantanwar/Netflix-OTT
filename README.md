# Netflix-OTT
Netflix OTT Clone is a responsive web application built using React.js that replicates the core functionality and user interface of Netflix. The project features user authentication with Firebase, dynamic content fetching through APIs using Axios, client-side routing with React Router DOM, and modern UI styling with SCSS. 
